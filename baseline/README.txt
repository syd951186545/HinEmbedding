如果是以拓扑结构并利用word2vec模型的，
可以一律使用path作为输入，然后输出一律都是embedding格式文件（伟英知道那个格式）。

输入格式-path列表：list（pathlist（），，，）----“列表的列表”或者别的更方便的也行
#因为输入word2vec中的就是序列，onehot编码什么的word2vec也一并做了。
所以就固定输入“游走路径的列表（多条路径）” ，所以先不管这些路径怎么来的，sampling那部分
同步去做就好了，我先做那部分，你
